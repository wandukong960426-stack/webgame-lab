# CLAUDE.md

이 파일은 Claude Code가 딴짓모아 저장소에서 작업할 때 따라야 하는 규칙이다.

## 세션 시작 체크리스트

작업을 시작하기 전에 반드시 다음을 읽는다.

1. `docs/PROJECT_STATE.md`
2. `docs/SITES_PARITY.md`
3. `docs/AI_HANDOFF.md`
4. 최근 커밋과 열린 Pull Request
5. 변경 대상 코드와 테스트

## 핵심 사실

- 공식 서비스 `https://ttanjitmoa.com`은 현재 ChatGPT Sites 운영본이다.
- 이 저장소는 별도의 Next.js 버전이며 아직 공식 운영본과 기능이 같지 않다.
- 사용자의 명시적 전환 승인 없이는 공식 도메인 연결, Production 배포, 기존 Sites 대체를 수행하지 않는다.
- 코드가 GitHub에 있거나 PR이 병합되었다는 이유로 “배포 완료”라고 보고하지 않는다.

## 작업 방식

- 새 작업은 항상 `claude/<짧은-작업명>` 브랜치에서 시작한다.
- `main` 직접 커밋·직접 병합·강제 푸시를 하지 않는다.
- 한 PR에는 하나의 목적만 담는다.
- `docs/AI_HANDOFF.md`에서 진행 중인 작업과 파일 충돌을 확인한다.
- 다른 AI가 Active로 표시한 파일을 동시에 수정하지 않는다.
- 대규모 리팩터링은 기능 변경과 분리한다.
- 비밀값을 코드, 로그, 이슈, PR에 남기지 않는다.

## 보존 및 품질 규칙

- 기존 게임, URL, SEO, 다국어, 저장 기능, 인증 태그, AdSense 설정을 임의 삭제하거나 축소하지 않는다.
- UI의 “고품질”을 정적 이미지나 장식 추가만으로 판단하지 않는다. 게임 상태 피드백, 조작 반응, 접근성, 모바일 터치, 성능과 오류 복구를 함께 검증한다.
- 새 의존성을 추가하기 전에 기존 구현으로 해결 가능한지 확인하고, 추가 사유를 PR에 기록한다.
- 완료 전 다음을 실행한다.

```bash
npm install
npm run typecheck
npm run build
npm run smoke
```

## 인수인계

작업 종료 시 `docs/AI_HANDOFF.md`에 다음을 남긴다.

- 작업 목적과 담당 AI
- 브랜치와 PR
- 변경 파일
- 실행한 검증과 결과
- 확인하지 못한 항목
- 다음 작업자가 해야 할 일

구현, 검증, 병합, Preview, 공식 Production은 각각 별도 상태로 표시한다.
